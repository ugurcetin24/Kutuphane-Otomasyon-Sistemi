@model KutuphaneSistemi.Models.Category

@{
    ViewData["Title"] = "Kategori Sil";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2>🗑️ Kategori Sil</h2>
    <a asp-action="Index" class="btn btn-secondary">← Listeye Dön</a>
</div>

<div class="alert alert-warning">
    <strong>Dikkat!</strong> Bu kategoriyi silmek istediğinizden emin misiniz? Bu işlem geri alınamaz.
</div>

@if (Model.Books.Any())
{
    <div class="alert alert-danger">
        <strong>⚠️ Uyarı:</strong> Bu kategoriye ait <strong>@Model.Books.Count</strong> kitap bulunmaktadır.
        Önce bu kitapları silin veya başka bir kategoriye atayın, ardından kategoriyi silebilirsiniz.
    </div>
}

<div class="card">
    <div class="card-header bg-danger text-white">
        <h5 class="mb-0">Silinecek Kategori Bilgileri</h5>
    </div>
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-3">Kategori Adı</dt>
            <dd class="col-sm-9 fw-bold">@Model.Name</dd>

            <dt class="col-sm-3">Açıklama</dt>
            <dd class="col-sm-9">@(Model.Description ?? "-")</dd>

            <dt class="col-sm-3">Kitap Sayısı</dt>
            <dd class="col-sm-9">
                <span class="badge bg-@(Model.Books.Any() ? "danger" : "success")">@Model.Books.Count kitap</span>
            </dd>
        </dl>
    </div>
    <div class="card-footer">
        @if (!Model.Books.Any())
        {
            <form asp-action="Delete" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="Id" />
                <button type="submit" class="btn btn-danger">Evet, Sil</button>
                <a asp-action="Index" class="btn btn-secondary ms-2">Vazgeç</a>
            </form>
        }
        else
        {
            <a asp-action="Index" class="btn btn-secondary">Geri Dön</a>
        }
    </div>
</div>
